services:
  - type: web
    name: projeto-suprimentos-dashboard
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app_dashboard.py --server.port=$PORT --server.address=0.0.0.0
